<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.vertx.microservices</groupId>
  <artifactId>samples</artifactId>
  <version>1.2-SNAPSHOT</version>

  <packaging>pom</packaging>

  <properties>
    <vertx.version>3.3.3</vertx.version>
    <slf4j.version>1.7.21</slf4j.version>
    <mvnco.version>3.6.0</mvnco.version>
    <dockm.version>0.18.1</dockm.version>
    <hazelcast.version>3.7.2</hazelcast.version>
    <jclouds.version>1.9.2</jclouds.version>
    <hazelcast-kubernetes-version>0.9.3</hazelcast-kubernetes-version>
  </properties>

  <modules>
    <module>aggregation-http</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.vertx</groupId>
        <artifactId>vertx-dependencies</artifactId>
        <version>${vertx.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>io.vertx</groupId>
      <artifactId>vertx-core</artifactId>
    </dependency>
    <dependency>
      <groupId>io.vertx</groupId>
      <artifactId>vertx-web</artifactId>
    </dependency>
    <!--dependency>
      <groupId>io.vertx</groupId>
      <artifactId>vertx-circuit-breaker</artifactId>
      <version>${vertx.version}</version>
    </dependency>
    <dependency>
      <groupId>io.vertx</groupId>
      <artifactId>vertx-service-discovery</artifactId>
      <version>${vertx.version}</version>
    </dependency-->
    <dependency>
      <groupId>io.vertx</groupId>
      <artifactId>vertx-hazelcast</artifactId>
      <version>${vertx.version}</version>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${mvnco.version}</version>
        <configuration>
          <source>1.8</source>
          <target>1.8</target>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>openshift</id>
      <properties>
        <docker.group.name>voivi</docker.group.name>
        <docker.image>${docker.group.name}/${project.artifactId}:${project.version}</docker.image>
        <fabric8.dockerUser>fabric8/</fabric8.dockerUser>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>io.fabric8</groupId>
            <artifactId>docker-maven-plugin</artifactId>
            <version>${dockm.version}</version>
            <configuration>
              <images>
                <image>
                  <name>${docker.image}</name>
                  <build>
                    <from>anapsix/alpine-java:jre8</from>
                    <tags>
                      <tag>latest</tag>
                    </tags>
                    <ports>
                      <port>8080</port>
                      <port>5701</port>
                    </ports>
                    <workdir>/opt</workdir>
                    <entryPoint>
                      <exec>
                        <arg>java</arg>
                        <arg>-jar</arg>
                        <arg>${project.artifactId}-${project.version}-fat.jar</arg>
                        <arg>-cluster</arg>
                        <arg>-conf</arg>
                        <arg>config.json</arg>
                        <!--arg>-cp</arg>
                        <arg>.</arg-->
                      </exec>
                    </entryPoint>
                    <runCmds>
                      <runcmd>chmod -R 777 /opt &amp;&amp; chmod -R 777 /opt/*</runcmd>
                    </runCmds>
                    <assembly>
                      <basedir>/</basedir>
                      <inline>
                        <files>
                          <file>
                            <source>${project.build.directory}/${project.artifactId}-${project.version}-fat.jar</source>
                            <outputDirectory>/opt</outputDirectory>
                            <fileMode>0755</fileMode>
                          </file>
                        </files>
                        <fileSets>
                          <fileSet>
                            <directory>${project.basedir}/src/openshift/</directory>
                            <outputDirectory>/opt</outputDirectory>
                            <fileMode>0755</fileMode>
                          </fileSet>
                        </fileSets>
                      </inline>
                    </assembly>
                  </build>
                  <run>
                    <ports>
                      <port>http.port:8080</port>
                      <port>eventbus.port:5701</port>
                    </ports>
                  </run>
                </image>
              </images>
            </configuration>
          </plugin>
          <plugin>
            <groupId>io.fabric8</groupId>
            <artifactId>fabric8-maven-plugin</artifactId>
            <version>2.2.96</version>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
